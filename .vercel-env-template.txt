# Copy these to Vercel Environment Variables
# Dashboard → Settings → Environment Variables

# Better Auth Core
BETTER_AUTH_SECRET=<run: openssl rand -base64 32>
BETTER_AUTH_URL=https://your-app.vercel.app

# AWS Cognito (get from CDK outputs after deployment)
COGNITO_CLIENT_ID=<from CDK output>
COGNITO_CLIENT_SECRET=<from AWS Console → Cognito → App Client → Show secret>
COGNITO_DOMAIN=<from CDK output, e.g., authly-prod.auth.us-east-1.amazoncognito.com>
COGNITO_USER_POOL_ID=<from CDK output>
AWS_REGION=<from CDK output, e.g., us-east-1>

# Database (choose one)
DATABASE_URL=<from Vercel Postgres / Neon / Supabase>


# OAuth Providers (optional, if using)
GITHUB_CLIENT_ID=<from GitHub Settings → Developer settings → OAuth Apps>
GITHUB_CLIENT_SECRET=<from GitHub OAuth App>
GOOGLE_CLIENT_ID=<from Google Cloud Console>
GOOGLE_CLIENT_SECRET=<from Google Cloud Console>

# AWS SES for Emails (optional, if using)
AWS_SES_REGION=us-east-1
AWS_ACCESS_KEY_ID=<from AWS IAM>
AWS_SECRET_ACCESS_KEY=<from AWS IAM>

# Environment
NODE_ENV=production

